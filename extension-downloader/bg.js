// Service worker mínimo. Existe só pra dar identidade estável à
// extensão (e permitir futuras melhorias tipo menu de contexto).
chrome.runtime.onInstalled.addListener(() => {
  // nada a fazer por enquanto
});
